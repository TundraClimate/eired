[package]
name = "tests"
version = "0.0.0"
publish = false
edition.workspace = true

[dependencies]
eired-display = { path = "../eired-display" }
eired-runtime = { path = "../eired-runtime" }
crossbeam.workspace = true
crossterm.workspace = true
